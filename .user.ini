; Raises the effective upload cap for fee/payment proof screenshots above
; the app's own 5MB limit (children-enrollment.php's $appMaxProofBytes),
; so PHP's ini limits are no longer the binding constraint.
upload_max_filesize = 10M
post_max_size = 12M
