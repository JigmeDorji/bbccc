<?php
// Retired: superseded by parent-fees.php (PCM fee system).
// Kept as a redirect so old bookmarks/links don't silently write into the
// unmonitored legacy `payments` table.
header("Location: parent-fees");
exit;
